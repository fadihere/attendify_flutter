  //  bool isFakeLocation =
  //                     await DetectFakeLocation().detectFakeLocation();
  //                 log("::requestApproved::${state.lastInModel?.requestApproved}");
  //                 if (isFakeLocation) {
  //                   showToast(msg: "Mock location is not allowed");
  //                 } else {
  //                   homeBloc.add(ClockedEvent(
  //                     lastIn: homeBloc.state.lastInModel!,
  //                     isClockedIn: isClockedIn,
  //                     file: File(path),
  //                     user: authBloc.state.user!,
  //                     lat: locationBloc.state.lat!,
  //                     lon: locationBloc.state.long!,
  //                     isAtOffice: locationBloc.state.isAtOffice,
  //                     isApproved: state.lastInModel?.requestApproved == true,
  //                   ));
  //                 }

  //