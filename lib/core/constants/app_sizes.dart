import 'package:flutter/material.dart';

class AppSize {
  static const side = 14.0;
  static const sidePadding = EdgeInsets.symmetric(horizontal: side);
  static const overallPadding = EdgeInsets.all(side);
}
